# 05 — Implementation Plan (Roadmap · Tasks · Progress)

> The **live execution tracker**. Update this every work session (task board + build log).
> **Last updated:** 2026-06-27 · Parent: [SPEC.md](SPEC.md)

## 1. Phase Roadmap
| Phase | Goal | Exit criteria |
|-------|------|---------------|
| **P0** | Toolchain setup | ESP-IDF 5.2.1 installed; repo cloned w/ submodules; `idf.py` works |
| **P1** | Flash hello-world | `examples/helloworld` (or `lvgl_demo`) builds + flashes; **display + knob verified** |
| **P2** | WiFi + provisioning | Device connects to WiFi; first-run provisioning captures WiFi creds |
| **P3** | Fetch + parse usage | `GET /api/oauth/usage` works on-device; 5h/7d parsed into state |
| **P4** | Service status | `status.claude.com` parsed; status screen reflects incidents |
| **P5** | LVGL screens | 5h / 7d / per-model / status screens render; knob navigation |
| **P6** | Token security | Token captured in provisioning; AES-256-GCM + PIN in NVS |
| **P7** | Polish & power | Error/stale states, brightness, battery, poll-interval config |
| **P8** | Stretch | Per-model buckets; **extended-glyph font (FR11)**; **Lucide icons (FR12)**; **splash/ambient animation (FR13)** — see 02 §10 |
| **P9** | Settings & Power UX | Charging-icon fix (DEF1); **Settings screen (FR14)** with **brightness (FR15)** + **power off (FR16)** — feasibility done, see 02 §12 |
| **P10** | UI restyle & power polish | Legibility/semantic UI redesign (terracotta titles, brighter text, amber incident — **07**); RGB LED **off in steady state** (D31) |
| **P11** | Spoken alerts | Speak 5H-usage + battery threshold crossings via the ES8311 speaker — offline pre-recorded clips (FR18, D32) |

## 2. Task Board
> Status: ⬜ todo · 🟡 doing · ✅ done

| ID | Task | Phase | Status | Notes |
|----|------|-------|--------|-------|
| T1 | Install ESP-IDF 5.2.1 toolchain (EIM-CLI) | P0 | ✅ | v5.2.1 @ `C:\esp\v5.2.1`; tools @ `C:\Espressif`; activate via EIM profile (04 §3) |
| T2 | Clone `SenseCAP-Watcher-Firmware` + submodules | P0 | ✅ | At `C:\esp\SenseCAP-Watcher-Firmware` (448 MB, submodules ok). `references/OSHW-*` = hardware repo, ≠ firmware. |
| T3 | Build `examples/helloworld` (build-only) | P1 | ✅ | Compiled on 5.2.1 (exit 0); full BSP + LVGL + managed components OK. |
| T-BK | **Back up device identity** (`nvsfactory`@0x9000 + full 32 MB dump + eFuse) | P1 | ✅ | Done & verified → `C:\esp\watcher_backup` (32 MB exact; 0x9000 cross-checks nvsfactory). See 04 §6.1 |
| T3f | Flash `helloworld` + verify boot | P1 | ✅ | Flashed @ COM18; serial confirms ESP-IDF 5.2.1 boot, 8 MB PSRAM, BSP/I²C up (touch 0x14, IO-exp 0x21, RTC 0x51), "Hello World" ~1 Hz. |
| T3g | Build + flash `lvgl_demo` (round screen) | P1 | ✅ | Flashed @ COM18; serial confirms LVGL task + **SPD2010 LCD** + touch + knob (GPIO41/42) all init OK. |
| T4 | Verify display renders + knob input works | P1 | ✅ | User-confirmed: **screen ✅, touch ✅, knob rotate+press ✅** (via `lvgl_encoder_demo`, `group cnt:6`). P1 hardware fully verified. |
| T5 | Bring up WiFi connect (STA, dev creds) | P2 | ✅ | Connected (WPA2, rssi -10), got IP 192.168.0.202. Project @ `C:\esp\watcher-claude-usage`. |
| T5b | HTTPS smoke test — GET `status.claude.com` (no auth) | P2 | ✅ | Cert validated (CA bundle), **HTTP 200**, parsed `status.json` ("All Systems Operational"). Full net path proven. |
| T6 | First-run provisioning (WiFi + token) | P6 | ✅ | **Works & verified (SoftAP+httpd+DNS up; SETUP screen rendered).** `provisioning.c/.h`: captive-portal — open SoftAP `Watcher-Setup`, captive DNS (tiny UDP responder → 192.168.4.1), `esp_http_server` serving a styled form (SSID/pass/token/version/poll) + catch-all GET. POST `/save` → urldecode → `config_save` → reboot. `ui_show_setup()` shows on-screen join instructions. **Real-phone round-trip user-confirmed (2026-06-24)** after raising `CONFIG_HTTPD_MAX_REQ_HDR_LEN` 512→2048 (phone header block exceeded the default). Re-provision: hold knob ~3 s (LVGL encoder long-press, threshold raised to 3000 ms) **or** hold knob at boot → `config_factory_reset` (knob polarity verified: released=high). Phone round-trip is the user's manual step. |
| T7 | HTTPS GET `/api/oauth/usage` + headers | P3 | 🔴 | **Blocked:** HTTP 403 `permission_error` — `claude setup-token` lacks `user:profile` scope (D11). Network/TLS path proven; headers/token wired. |
| T7b | Fallback: `POST /v1/messages` probe → rate-limit headers (5h/7d) | P3 | ✅ | **Works.** HTTP 200; live data parsed: **5h 23%, 7d 8%** + per-window status=allowed + reset epochs + representative-claim/overage headers. |
| T8 | Parse usage → state model (poll loop + SNTP) | P3 | 🟡 | Header→util%/reset parsing done. Need: state struct, periodic poll loop + 429 backoff, SNTP time for reset countdowns. |
| T9 | Fetch + parse `status.claude.com` | P4 | ✅ | **Works (user-confirmed render).** `claude_status.c`: GET `summary.json` → cJSON → `service_status_t`; SERVICE screen shows real health (color by indicator) + incident text. On-device: HTTP 200, `indicator=none "All Systems Operational"`, 1 incident parsed. |
| T9b | ASCII-transliterate incident text (font shim) | P4 | ✅ | `ascii_sanitize()` in `ui.c` maps UTF-8 curly quotes/dashes/ellipsis → ASCII (BSP fonts ASCII-only). Fixes `We⎕ve`→`We've`. Superseded by T16 (FR11). |
| T10 | LVGL: 5h + 7d screens | P5 | ✅ | Iter-1 stable (user-confirmed): concentric arcs (5h/7d) + center % + RGB-LED cue + 60s poll. Fix: LVGL pinned to CPU1. |
| T11 | LVGL: status screen | P5 | ✅ | SERVICE screen (unified-status + 5H/7D %). Per-model dropped (D11). |
| T12 | Knob navigation between screens | P5 | ✅ | Encoder→lv_group; rotate cycles 5H/7D/SERVICE (user-confirmed). |
| T13 | Token capture + AES-256-GCM in NVS (boot-and-go, no PIN) | P6 | ✅ | **Works & verified (crypto roundtrip survives reboot; runtime auth → live 5h/7d).** `secure_store.c/.h`: AES-256-GCM via mbedTLS, key = SHA-256(MAC ‖ random salt ‖ compiled pepper), self-contained blob `[salt16‖nonce12‖tag16‖ct]`. `config.c/.h`: NVS ns `wcfg` — SSID/ver/poll plaintext, wifi-pass+token in the GCM blob; `config_load/save/factory_reset`. `claude_usage` + `wifi_connect` refactored to take token/creds at **runtime** (no compiled secrets). **PIN dropped** per user decision (boot-and-go software obfuscation): auto-decrypts every boot, no prompt. Threat model documented (defeats casual `strings`, NOT a determined same-chip dump; real at-rest = HW flash-enc, deliberately not burned). 02 §5. |
| T14 | Error/stale/offline states + 429 backoff | P7 | ✅ | **Done.** Poll loop tracks last-good time (`esp_timer`); on fetch fail keeps last-known arcs/% and shows **"stale Xm"** (orange) on the gauges (`ui_set_stale`), WiFi icon red when `wifi_is_connected()` is false (handler now auto-reconnects forever in the background), service health keeps last-known. **429 backoff:** `claude_usage` surfaces `http_status`; on 429 the interval doubles up to 600 s, resets to `cfg.poll_s` on success. Happy path on-device-verified; stale/429 are deterministic (not live-triggered headlessly). |
| T15 | Brightness, battery, configurable interval | P7 | ✅ | **Done & on-device-verified.** Battery: real `bsp_battery_is_present`/`get_percent`/`bsp_system_is_charging` each poll → `ui_set_battery` (screenshot shows the green charging bolt on USB; percent icon on a pack). **Earlier "no BSP battery API" note was wrong** — APIs exist (ADC self-inits; rail powered by `bsp_io_expander_init`). Brightness: **configurable static** `cfg.brightness` (portal field, default 80) → `bsp_lcd_brightness_set` at boot. Interval: `cfg.poll_s` (already wired). |
| T16 | **Extended-glyph font** (FR11) — `lv_font_conv` Montserrat + punctuation range; retire `ascii_sanitize` | P8 | ✅ | **Works (user moved on to T17).** `font_ext_16.c`/`font_ext_22.c` from LVGL's Montserrat-Medium.ttf, range `0xB7,0x2013-14,0x2018-19,0x201C-1D,0x2026`. SERVICE labels use them; shim removed. Confirmed LVGL 8.4 → **zero patching** needed. +20 KB, 53% free. |
| T17 | **Lucide icons** (FR12) — SVG→alpha-8bit C arrays; WiFi/health/battery/zap glyphs | P8 | ✅ | **Works (user-confirmed render).** 12 icons via lucide-static→sharp→`LV_IMG_CF_ALPHA_8BIT` (`icons_lucide.c/.h`); recolored at runtime. WiFi+battery top-bar (all screens), health glyph on SERVICE, zap on gauges. Battery = grey placeholder (`ui_set_battery()` hook ready) until P7. |
| T17b | **Reposition WiFi/battery top-bar icons** | P8 | ✅ | **Fixed & screenshot-verified.** Icons moved from the top edge (`TOP_MID` y=22, which sat on the 270° ring's top arc @y≈26–54) down into the open hub (y=64) → clear of the arc on both gauges, above titles on all 3 screens. The "pale WiFi on SERVICE" was a **perception artifact** (downscaled green-on-black read as grey) — pixel sampling all 3 shots shows WiFi = identical green `(74,174,82)` everywhere; recolor is correct, no defect. Battery = grey `(156,157,156)` placeholder (P7). 02 §10.2. |
| T18 | **Splash/ambient animation** (FR13) — pixel-art engine, mood by usage level | P8 | ✅ | **Works & screenshot-verified.** 4th knob screen **CLAWD**: 20×20 palette-indexed pixel-art engine `main/splash.c/.h` (ported from Clawdmeter), 13 claudepix creature anims in `main/splash_animations.h` (generated by reusing the reference `convert_to_c.js` on the **already-vendored** `claudepix_data/*.json` — no scraping). 400×400 `lv_canvas` in PSRAM, `lv_timer` (~30Hz) advances frames + auto-rotates ~20s within a usage-driven **4-level mood group** (idle<25 / normal<50 / active<80 / heavy≥80, driver=`max(5h,7d)`). Palette→`lv_color_make` at runtime = byte-swap-agnostic; brand remap `#cd7f6a→#d97757` baked in. +177KB (50% free). **Host fix:** `grab_screens.py` loop cap 3→4 (was silently dropping the 4th frame). 02 §10.3. |
| T20 | **Charging-icon fix (DEF1) + battery badge rework** — use `VBUS_IN_DET` not `CHRG_DET` | P9 | ✅ | **Done & on-device-verified (user-confirmed unplug/replug + render).** Beyond the trivial fix: rebuilt the battery badge as a **self-driven `lv_timer`** in `ui.c` (`power_timer_cb`, 1 s) reading `BSP_PWR_VBUS_IN_DET`(==0→on USB) every tick + `bsp_battery_get_percent()` every 30 s / on USB transition — **decoupled from the 60–600 s poll loop** (fixed: re-plug didn't update). Dropped `bsp_battery_is_present()` gating (mirrors stock; fixed: unplugged showed empty grey glyph). Added a numeric **`NN%`** label under the icon (font_ext_16, recolored by level). Removed the battery block from `main.c` poll loop; removed `ui_set_battery` from `ui.h`. D19 (signal) + D22 (architecture). 02 §12.1. |
| T21 | **Settings screen scaffold (FR14)** | P9 | ✅ | **Done & user-confirmed.** `build_settings()` in `ui.c` → 5th screen `set_scr` (title + brightness slider + power btn); added to encoder `grp` after CLAWD, plus `bri_slider`+`pwr_btn` as members. `settings_member_focus_cb` loads SETTINGS when the slider/button is focused (fixes wrap-from-5H landing on a hidden member). `ui_screenshot` 4→5 (+"SETTINGS"); `grab_screens.py` cap 4→5. No top-bar (like CLAWD). |
| T22 | **Brightness knob (FR15)** — press-to-edit slider | P9 | ✅ | **Done & on-device-confirmed.** `lv_slider` (5–100) editable group member: press-to-edit, rotate adjusts, live `bsp_lcd_brightness_set` on `VALUE_CHANGED`; **persist on `DEFOCUSED`** via new `config_set_brightness()` (writes NVS `bri` only — no token re-encrypt). Slider inits from `cfg.brightness` (passed via `ui_init(int)`). MUST-FIX resolved: reprovision long-press kept on the 5 screens (suppressed only while the slider itself is focused — documented, not relocated). |
| T23 | **Power off (FR16)** + confirm | P9 | ✅ | **Done & on-device-confirmed (reboot-on-USB tested).** Non-editable `lv_btn` → custom top-layer confirm modal (`lv_layer_top()`, temporary encoder group, **Cancel default-focused**). Confirm gates on `VBUS_IN_DET`: USB present → `esp_restart()` (relabelled **"Reboot"** live by `power_timer_cb`); on battery → `bsp_system_shutdown()` (wake = hold wheel ~3 s / USB). Modal shows wake instructions. |
| T19 | **Automated UI verification** (devex) — remove manual on-device screen inspection | P8/tooling | ✅ | **Option B works (chose B over the MCP sim).** Built-in **`lv_snapshot`** (`LV_USE_SNAPSHOT` already =y) → `screenshot.c` streams each screen as RLE-hex over UART (`ui_screenshot()` once per boot) → host `tools/grab_screens.py` (pyserial+stdlib zlib, **no MCP**) writes `shot_<name>.png` → assistant `Read`s them. Captured all 3 screens (412×412 exact); confirmed T16/T17/P4 visually + the T17b overlap, all without manual inspection. See 02 §11. |
| T24 | **Fix DEF2 wake → re-login (D25)** — remove boot factory-reset trigger | P9+ | ✅ | **Done & on-device-confirmed (2026-06-25).** Deleted `knob_held_at_boot()` + its call in `main.c` `app_main()` so a provisioned device boots straight to normal operation on wake (NVS creds persist; they were being wiped by accident). Kept the in-use 3 s knob long-press (`reprovision_cb`) as the sole deliberate reprovision path. Root cause: hold-wheel-to-wake (T23/D20) collided with hold-knob-at-boot reprovision (P6/D17). Built clean (47% free), flashed COM18. 02 §12.2 + §5 risks. **Verified on battery (user-confirmed): power off → hold wheel to wake → gauges return, no portal.** |
| T25 | **UI redesign** (07) — semantic palette, terracotta titles, brighter text, incident amber | P10 | ✅ | **P1 live & user-approved (2026-06-27).** `ui.c`+`sdkconfig` (MONTSERRAT_28): `COL_*` palette, terracotta `#D97757` titles@28, grey→`#C8C8C8` secondary text @22, **SERVICE incident decoupled→amber not green**, fixed-accent terracotta zap, terracotta slider. P2 size-enlargement (40/80 icons, font_ext_28) tried & **reverted as too big** (D30). Added `tools/icontool/` (lucide→sharp→alpha-8 regen). 07 §13/§14/§15; D26–D30. |
| T26 | **RGB LED off in steady state** (D31) — stop the always-on level cue | P10 | ✅ | **Done & user-confirmed off (2026-06-27).** `main.c`: removed the per-poll `bsp_rgb_set` usage-level/error recolor (kept 429 backoff) + `bsp_rgb_set(0,0,0)` once before the poll loop. WS2812 on GPIO40 now dark while running; boot magenta + connecting blue kept. Built/flashed COM18. |
| T27 | **Spoken alerts (FR18, D32)** — speak 5H-usage + battery threshold crossings | P11 | ✅ | **Done & user-confirmed audible (2026-06-27).** Validated ES8311 speaker (04 Audio). Offline TTS: 10 "Zira" clips (16 kHz mono PCM, peak-normalized + soft-limited, vol 95) via `tools/audio/{gen_clips.ps1, wav_to_c.py}` → `main/speak_clips.c`; `main/speak.c` plays them in a dedicated task (**chunked** I2S writes — a single big write tripped the Task-WDT). Announces 5H rising 25/50/75/90/100 + battery falling 50/30/20/10/5 (muted on USB), prime-silent on boot + ±3 % hysteresis; hooked into the poll loop (usage) + `ui.c power_timer_cb` (battery). ⚠️ App bin now ~2.49 MB / 3 MB partition (21 % free). 02 §13. |

## 3. Current Focus
- **✅ P11 (Spoken alerts, FR18/D32) — 2026-06-27, user-confirmed audible.** The device speaks 5H-usage rising (25/50/75/90/100) + battery falling (50/30/20/10/5, muted on USB) via the ES8311 speaker, using offline pre-recorded clips (`tools/audio/` → `speak_clips.c`; `speak.c`). Loud (vol 95 + normalization). Silent on boot (threshold-crossing only). ⚠️ App bin ~2.49 MB / 3 MB partition (21 % free) — audio clips are large.
- **✅ P10 (UI restyle + LED power polish) — 2026-06-27.** UI redesign (**07**) P1 live & **user-approved**: semantic palette, terracotta titles@28, brighter `#C8C8C8` text, SERVICE incident **amber-not-green**, terracotta zap/slider (the P2 size-enlargement was tried & **reverted as too big**, D30). RGB **LED off in steady state** (D31) — user-confirmed off. Optional P3 UI items remain (07 §9: R8 status-word, R10 fetch-error look, R11 layout, 5 % brightness floor). **Possible next feature:** "speak" usage/battery via the ES8311 speaker (validated present; 04 Audio row).
- **✅ T24 (DEF2) DONE & on-device-confirmed (2026-06-25)** — removed the boot `knob_held_at_boot()` factory-reset trigger (D25) so waking from a true power-off no longer re-asks WiFi login. **No open defects remaining** — only the optional backlog below.
- **✅ P9 (Settings & Power UX) COMPLETE — all on-device-confirmed (2026-06-25). P0–P9 + T24 all done.** T20 (charging fix + battery badge rework: self-driven timer, numeric % beside the icon, prompt plug/unplug), T21 (Settings 5th screen), T22 (press-to-edit brightness slider, persisted), T23 (power-off/reboot with confirm + USB-relabel). SERVICE top-bar spacing resolved by removing the "SERVICE" title and keeping the battery % below its icon (D24, user preference). No required work remaining — only backlog below.
- **Full picture:** the Watcher provisions over a phone captive portal (real-phone confirmed), stores the OAuth token AES-256-GCM-encrypted in NVS (boot-and-go, no PIN, no secrets in firmware), then autonomously shows 5H/7D usage + reset countdowns + real Claude service status + WiFi/battery(%)/health/zap icons + a CLAWD creature screen (mood by usage) + a SETTINGS screen (brightness + power-off). On failure it keeps last-known data and shows stale/offline; respects 429 with exponential backoff; real battery + charging; configurable brightness (live, on-device) + poll interval via the portal.
- **Devex:** **T19 ✅** on-device screenshot — `idf.py flash` then `python tools/grab_screens.py COM18 <out> 100` → PNGs (all 4 screens; `ui_screenshot_one()` grabs the active screen). Serial: `scratchpad/cap.py COM18 <secs>` (reliable RTS reset).
- **Possible follow-ups (backlog, not required):** auto-dim/sleep on inactivity; per-model buckets if a `user:profile` token path appears (D11); live-trigger tests for stale/429; quieten the BSP per-poll battery voltage logs.

## 4. Build / Run Log (changelog)
| Date | Entry |
|------|-------|
| 2026-06-27 | **✅ P11 — Spoken alerts (FR18, D32), user-confirmed audible.** Validated the ES8311 speaker (04 Audio). Offline TTS path: SAPI "Zira" → 10 clips (16 kHz mono PCM, peak-normalized + tanh soft-limited for loudness, vol 95) via `tools/audio/{gen_clips.ps1, wav_to_c.py}` → `main/speak_clips.c` (~731 KB); `main/speak.c` = dedicated task, **chunked** `bsp_i2s_write` (single big write tripped Task-WDT; also dropped a redundant `bsp_codec_set_fs` that disabled the channel). Threshold logic: 5H rising 25/50/75/90/100 + battery falling 50/30/20/10/5 (USB-muted), prime-silent-on-boot + ±3 % hysteresis; hooks in `main.c` (usage) + `ui.c power_timer_cb` (battery). Flash needed a USB replug once (chip went unresponsive). 02 §13. |
| 2026-06-27 | **✅ P10 — UI redesign + LED power polish (07 + SPEC D26–D31).** UI: `COL_*` palette, terracotta titles@28, brighter `#C8C8C8` text, SERVICE incident **amber-not-green**, terracotta zap/slider (P1, user-approved); P2 size-enlargement (40/80 icons + `font_ext_28` 28 px status) tried & **reverted as too big** (D30); added `tools/icontool/` (lucide→sharp→alpha-8 regen). LED: removed the always-on per-poll RGB level cue → `bsp_rgb_set(0,0,0)` in steady state (D31), **user-confirmed off**. All built/flashed COM18, screenshot/eyeball-verified. Audit: refreshed 01/02/04/05/SPEC + claude-flow `sensecap-watcher` memory (added spec-07; D1–D31). 04 gained the **Audio/ES8311 speaker** hardware row (validated for a future "speak" feature). |
| 2026-06-25 | **✅ T24 — DEF2 FIXED & on-device-confirmed.** Removed `knob_held_at_boot()` + its `app_main()` call from `main.c` (added an inline comment so it isn't re-added) so a provisioned device boots straight to normal op on wake — the boot factory-reset no longer collides with the hold-wheel-to-wake gesture (T23/D20). In-use 3 s knob long-press (`reprovision_cb`) kept as the sole reprovision path. `idf.py build` clean (47% free, no warnings) → flashed COM18. **User confirmed on battery: power off → hold wheel → gauges return, no WiFi portal.** P0–P9 + T24 feature-complete; no open defects. **Lesson: a wake-by-button gesture must not share a trigger with a boot-time reset check.** |
| 2026-06-25 | **Found DEF2 + decided fix (D25) — no code yet (queued T24, next session).** User report: after a true power-off, every wake re-asks WiFi login. Root cause traced to `main.c` `knob_held_at_boot()` — the hold-wheel-to-wake gesture (T23/D20) is still active as the device boots, so the boot reprovision check (P6/D17) factory-resets the NVS creds. Decided: remove the boot trigger (keep in-use long-press). Logged in 01 (DEF2), 02 §12.2, SPEC §5/D25/changelog. **Lesson: a wake-by-button gesture must not share a trigger with a boot-time reset check.** |
| 2026-06-25 | **UI polish (D24, supersedes %-placement of D23).** User preferred reverting the status-bar tweak: battery % returned to **below** the icon (`batt_lbl` → `LV_ALIGN_OUT_BOTTOM_MID` in `add_topbar` + `render_battery`) and the **"SERVICE" title removed** from `build_status()`. No title ⇒ the % below the icon has clear space; health glyph + "All Systems Operational" still identify the screen. `ui.c` only; screenshot-verified (5H + SERVICE). |
| 2026-06-25 | **✅ P9 COMPLETE — T21+T22+T23 done & on-device-confirmed (full Settings screen).** New 5th knob screen **SETTINGS** (`build_settings()` in `ui.c`): **brightness** `lv_slider` (5–100, editable press-to-edit → live `bsp_lcd_brightness_set`, persisted on `DEFOCUSED` via new `config_set_brightness()` writing NVS `bri` only — no token re-encrypt; inits from `cfg.brightness` via new `ui_init(int)` signature) + **power** `lv_btn` → custom top-layer confirm modal (`lv_layer_top()`, temporary encoder group, **Cancel default-focused**); confirm gates on `VBUS_IN_DET` → on USB `esp_restart()` (button live-relabelled **"Reboot"** by `power_timer_cb`), on battery `bsp_system_shutdown()` (+ wake instructions). `settings_member_focus_cb` keeps SETTINGS visible when the slider/button is focused. `s_enc` captured for the modal group swap. `ui_screenshot` 4→5, `grab_screens.py` cap 4→5. MUST-FIX resolved: reprovision long-press kept on the 5 screens (suppressed only on the focused slider — documented). Build 47% free; user confirmed brightness + reboot/shutdown. **Then UI tweak (user feedback):** battery **% moved beside the icon** (`LV_ALIGN_OUT_RIGHT_MID`) instead of below — fixes SERVICE-title crowding on the round panel; screenshot-verified on 5H + SERVICE. D20/D21/D23. |
| 2026-06-25 | **✅ T20 (DEF1 + battery rework) — DONE & user-confirmed.** Step 1: charging fix per D19 (`bsp_system_is_charging` reads `CHRG_DET` inverted → derive `on_usb = bsp_exp_io_get_level(BSP_PWR_VBUS_IN_DET)==0`). Built/flashed COM18; user confirmed icon clears on unplug, **but** re-plug didn't update + unplugged glyph was empty + wanted %. Step 2 (D22): rebuilt the badge as a **self-driven `lv_timer`** `power_timer_cb` in `ui.c` (1 s; reads VBUS every tick, `bsp_battery_get_percent()` every 30 s + on USB transition) — mirrors stock `app_device_info.c:1382/1428`; **decoupled from the poll loop** so plug/unplug reflects in ~1 s both ways. Dropped `bsp_battery_is_present()` gating; added numeric **`NN%`** label under the icon (`font_ext_16`, recolored: green charge / red<15 / orange<50 / green). Removed battery block from `main.c` loop; removed `ui_set_battery` from `ui.h`. Fix: `txt[8]→[16]` (`-Werror=format-truncation`). Build clean 47% free; screenshot-verified `100%` + charging bolt on all screens (no SERVICE-title collision); user confirmed unplug/re-plug live. |
| 2026-06-23 | Spec set created; plan drafted. No firmware work started yet. |
| 2026-06-24 | **T1 ✅** ESP-IDF v5.2.1 installed via **EIM-CLI**. `idf.py` verified (venv `C:\Espressif\tools\python\v5.2.1\venv`), esp32s3 target OK. Activate: `. C:\Espressif\tools\Microsoft.v5.2.1.PowerShell_profile.ps1`. **Gotcha:** system `python` is the Windows Store stub → bare `esp-idf\export.ps1` fails ("Python was not found"); must use the EIM profile. IDF_TOOLS_PATH = `C:\Espressif` (not `~/.espressif`). |
| 2026-06-24 | **Smoke build ✅** — ESP-IDF `get-started/hello_world` built at `C:\esp\hello_test` (exit 0, bin produced) → confirms compiler+ninja+image-gen pipeline. |
| 2026-06-24 | **T2 ✅** Cloned `SenseCAP-Watcher-Firmware` (448 MB, submodules: rlottie, openai-realtime) → `C:\esp\SenseCAP-Watcher-Firmware`. Examples (`helloworld`, `lvgl_demo`, `lvgl_encoder_demo`, `knob_rgb`, `factory_firmware`, …) all use local `components/sensecap-watcher` BSP and declare `idf: ">=5.0"` (no upper bound) → **5.2.1 OK** (D7 risk lower than feared). |
| 2026-06-24 | **T3 build ✅** `examples/helloworld` compiled on ESP-IDF 5.2.1 (exit 0; full `sensecap-watcher` BSP + LVGL + managed components) → firmware↔toolchain path proven; only flashing remains untested. |
| 2026-06-24 | **Pre-flash safety:** decoded factory partition table → `nvsfactory`@0x9000 (200K) holds unique SenseCraft credentials (SN/EUI/DEVICE_KEY/ACCESS_KEY/AES_KEY), **absent from the public factory image** → **mandatory backup before flashing** (task T-BK; procedure in 04 §6.1; Decision D9). |
| 2026-06-24 | **T-BK running:** `nvsfactory.bin` (0x9000, 200K, verified non-blank ✅) + `efuse_summary.txt` saved to `C:\esp\watcher_backup` (outside repo). Port = **COM18** (COM19 no response), baud **460800** (921600 corrupts). eFuse: flash-encryption + secure-boot **disabled** → dump valid + flashing unhindered. Full 32 MB dump running as resumable 4 MB chunks. |
| 2026-06-24 | **T-BK ✅ complete & verified.** `C:\esp\watcher_backup\`: `nvsfactory.bin` (200K), `watcher_full_32MB.bin` (32 MB exact), `efuse_summary.txt`. Cross-check: full image @0x9000 == nvsfactory.bin (SHA256 match). Temp chunks removed. **Flashing now unblocked** (still held per user). |
| 2026-06-24 | **T3f ✅** `helloworld` flashed to COM18 (all regions hash-verified, hard-reset OK). Serial: ESP-IDF v5.2.1, 8 MB octal PSRAM @80 MHz, BSP I²C up (devices @0x14 touch / 0x18 / 0x21 IO-exp / 0x51 RTC), IO-expander `0xffd9`, "Hello World!" ~1 Hz. Flash used helloworld's own partition table; **0x9000 not written** (creds intact). Now building/flashing `lvgl_demo`. |
| 2026-06-24 | **T3g ✅** `lvgl_demo` (698 KB) flashed to COM18 (hash-verified). Serial: `LVGL: Starting LVGL task`, `spd2010: LCD panel create success v1.0.1` (**display = SPD2010**, round 412×412, RGB565/LV_COLOR_16_SWAP), knob init **GPIO41=A / 42=B** (IoT Knob 0.1.4) + IoT Button 3.2.3, **SPD2010 touch** create success, ~7.75 MB free heap. Full HW bring-up confirmed at driver level (awaiting user visual confirm = T4). |
| 2026-06-24 | **T4 partial:** user confirms round screen renders + **touch works**. Knob (rotate+click) does nothing in `lvgl_demo` — root cause: `lvgl_demo/main` never creates an `lv_group`/binds the encoder indev (grep: no encoder refs), whereas `lvgl_encoder_demo` does (`lv_indev_set_group`). Building `lvgl_encoder_demo` to verify knob HW. |
| 2026-06-24 | **lvgl_encoder_demo flashed** to COM18 (hash-verified). Serial: LCD+touch+knob init OK, `group cnt:6` (encoder bound to an lv_group of 6 objs). Awaiting user knob rotate/press test. |
| 2026-06-24 | **T4 ✅ / P1 COMPLETE.** User confirms knob **rotate + press both work** in lvgl_encoder_demo. Full HW verified: display (SPD2010 round 412×412), touch (SPD2010), knob (encoder GPIO41/42 + button). Toolchain→build→flash→boot→display→input all proven end-to-end. Next: **P2 WiFi**. |
| 2026-06-24 | **P2 started.** Scaffolded standalone project `C:\esp\watcher-claude-usage` (top CMakeLists, sdkconfig.defaults [32MB/OCT-PSRAM/cert-bundle], partitions.csv [nvs@0x9000 preserved], `main.c` + `wifi_connect.c` + `http_get.c`, git-ignored `secrets.h`). Minimal: WiFi STA via raw `esp_wifi` + HTTPS GET `status.claude.com` (cert bundle); **no BSP/LVGL yet**. Build validating compile. |
| 2026-06-24 | **T5 + T5b ✅ — P2 network path PROVEN.** Flashed `watcher-claude-usage` to COM18; serial: WiFi STA connected (WPA2, rssi -10) → IP 192.168.0.202 → `esp-x509-crt-bundle: Certificate validated` → **HTTP 200** from `status.claude.com/api/v2/status.json` ("All Systems Operational", 212 B parsed). On-device WiFi+DNS+TLS+HTTPS all confirmed. App at 0x40000; nvs@0x9000 preserved. |
| 2026-06-24 | **T7 → 403 (scope).** Authed `GET /api/oauth/usage` (token + Authorization/anthropic-beta/UA headers) returns **HTTP 403** `{"type":"permission_error","message":"OAuth token does not meet scope requirement user:profile"}`. The `claude setup-token` credential has inference scope only. status.claude.com still 200 OK (path fine). → Pivot to `/v1/messages` rate-limit-header fallback (D6/D11), pending user decision (T7b). |
| 2026-06-24 | Investigated full-login token (`~/.claude/.credentials.json`): HAS `user:profile` + refreshToken but access token expires ~hourly; on-device refresh risks breaking PC's Claude Code login via refresh-token rotation. **User chose the /v1/messages fallback (Option A).** |
| 2026-06-24 | **T7b ✅ — usage fetch WORKS (fallback).** `POST /v1/messages` (max_tokens:1 haiku) with setup-token → HTTP 200. Live headers: 5h util=0.23 (23%), 7d util=0.08 (8%), both status=allowed; resets 5h=1782288600 / 7d=1782507600 (epoch s). Also unified-status=allowed, representative-claim=five_hour, fallback-percentage=0.5, overage-status=rejected. **Full data path proven end-to-end with the autonomous setup-token.** |
| 2026-06-24 | **P5 started.** Added `sensecap-watcher` BSP to `watcher-claude-usage` (`main/idf_component.yml` override_path → sibling repo). New `ui.c` (LVGL: 2 concentric arcs 5h/7d + center %, color-by-level); refactored `claude_usage`→`usage_t`; `main.c` = io_expander→rgb→`bsp_lvgl_init`→`ui_init`→WiFi→60s poll (fetch→`ui_update`+RGB cue). sdkconfig += LVGL color-swap/fonts (montserrat 16/22/32/48). Building (full BSP+LVGL). |
| 2026-06-24 | **Build fix:** `main` had explicit `REQUIRES` → cut off from BSP's transitive `esp_io_expander.h`. Removed REQUIRES (main implicitly depends on all components, like the BSP examples) → builds clean (1.45 MB). |
| 2026-06-24 | **✅ P5 iter-1 WORKS — live usage on the round screen.** After pinning LVGL→CPU1 (`LVGL_PORT_TASK_AFFINITY_CPU1` + `FREERTOS_HZ=1000` + `LV_MEM_CUSTOM`): clean boot, no SPI/WDT errors, **user-confirmed STABLE render** — two concentric arcs (5h/7d) + center % + RGB LED, refreshing every 60 s. The Watcher now autonomously displays Claude Code usage, no host PC. (5h read 4% — window had reset.) |
| 2026-06-24 | **✅ P5 iter-2 (polish) WORKS** (user-confirmed). SNTP time sync (`esp_netif_sntp`, type `esp_sntp_config_t`) → "resets in Xh Ym" countdowns. 3 knob-navigable screens (5H gauge / 7D gauge / SERVICE status) — screens added to an `lv_group` bound to the encoder indev; rotate cycles them. Clean boot, no errors. (Fixed: SNTP config var type.) |
| 2026-06-24 | **✅ P4 / T9 WORKS — service status live** (user-confirmed render). New `claude_status.c`: unauthenticated GET `status.claude.com/api/v2/summary.json` → 6 KB PSRAM buf → **cJSON** → `service_status_t {indicator, description, incident_count, incident}`. `main.c` poll loop now fetches health each cycle (no quota cost); SERVICE screen repurposed from rate-limit `unified-status` to **real claude.com health** (color: none→green/minor→orange/major·critical→red/maint→blue) + first incident name (`+N more`) or "no incidents". Serial: HTTP 200 (4647 B), `indicator=none "All Systems Operational"`, incidents=1. Build clean (1.46 MB, 54% free). |
| 2026-06-24 | **✅ T9b — apostrophe/box fix.** Incident "We’ve…" rendered "We⎕ve" (BSP Montserrat = ASCII-only; U+2019 has no glyph). Added `ascii_sanitize()` in `ui.c`: minimal UTF-8 decode → transliterate curly quotes/dashes/ellipsis/nbsp to ASCII, drop other non-ASCII. Now "We've". Flashed COM18. **Superseded later by FR11 extended font (T16).** |
| 2026-06-24 | **✅ T19 (devex) — Option B on-device screenshot WORKS, no MCP.** Built-in `lv_snapshot` (`LV_USE_SNAPSHOT` was already =y) → new `screenshot.c` (`screenshot_dump_all`: snapshot each screen under the LVGL lock into PSRAM, then stream RLE-hex framed `@@SHOT/@@END` over UART with the lock released + logs muted → WDT-safe). `ui_screenshot()` fires once per boot from `main.c`. Host `tools/grab_screens.py` (ESP-IDF venv python: pyserial + stdlib zlib, **no PIL/MCP**) resets via RTS, parses frames, un-swaps RGB565 (`LV_COLOR_16_SWAP`) → PNG. Captured all 3 screens @412×412 (169,744 px exact); assistant `Read` the PNGs → visually re-confirmed T16 ("We've" apostrophe), T17 (icons), P4 (status) **and** the T17b arc-overlap, zero manual inspection. Chose B over the LVGL-9.2 MCP sim (faithful to our round RGB565 8.4 device). +~3 KB fw. |
| 2026-06-24 | **✅ T16 (FR11) extended font** done & flashed (user moved on = confirmed). `font_ext_16.c`/`font_ext_22.c` generated via `npx lv_font_conv@1.5.3` from LVGL's own `Montserrat-Medium.ttf`, range `0x20-0x7E,0xB7,0x2013-14,0x2018-19,0x201C-1D,0x2026`, bpp4/no-compress/format lvgl. SERVICE labels point at them; **`ascii_sanitize` shim removed** (raw UTF-8 now). Confirmed: LVGL 8.4 → generated files compile with **zero patching** (their `LVGL_VERSION_MAJOR==8` guards handle `.cache`/`.fallback`). +20 KB. |
| 2026-06-24 | **✅ T17 (FR12) Lucide icons** done & flashed (user-confirmed render). Pipeline: `lucide-static` (MIT) SVG → **sharp** raster (density 512) → alpha plane → **`LV_IMG_CF_ALPHA_8BIT`** C arrays (`icons_lucide.c/.h`, 12 icons, regen script `scratchpad/icontool/gen_icons.mjs`). Recolored at runtime via `lv_obj_set_style_img_recolor`. Placement: WiFi+battery top-bar on all 3 screens (`wifi_ic[]`/`batt_ic[]`), health glyph (circle-check/triangle-alert/circle-x/wrench) on SERVICE, zap on gauges. New `ui_set_wifi()`/`ui_set_battery()`. Battery = grey "unknown" placeholder (no BSP battery API → P7). Clean boot, no faults. +~17 KB. |
| 2026-06-24 | **T17b known issue (user):** WiFi/battery top-bar icons render at top-center as intended but **overlap the top of the circular usage arc/ring**. Logged for reposition (move into the bottom arc-gap or inward below the ring). Deferred. |
| 2026-06-24 | **T19 groundwork (devex):** user hit friction needing manual on-device screen inspection each iteration; found `references/Lvgl-mcp-esp32`. Read its README + checked feasibility: MSVC (VS2022 Community `cl.exe`) **present**; but the sim is **LVGL 9.2** (we're 8.4) and renders **square 32bpp** ≠ our round 412×412 RGB565 → preview-only, won't match device. On-device alt: LVGL 8.4 ships `lv_snapshot.h` (`LV_USE_SNAPSHOT=0`) → enableable for a Clawdmeter-style serial screenshot (ground-truth). Full eval = next session (T19). |
| 2026-06-24 | **Planning — P8 stretch features added to specs** (from `references/Clawdmeter`). FR11 extended-glyph font (punctuation-only, no CJK), FR12 Lucide icons, FR13 splash/ambient animation. Design in 02 §10; tasks T16–T18. **Key finding:** our LVGL is **8.4.0** (vendored in SenseCAP repo) vs Clawdmeter's LVGL 9 → `lv_font_conv`/image output drops in **without** the LVGL-9 patching its README requires. User decisions: font=punctuation-only, splash art source=deferred, sequence=per-phase. |
| 2026-06-25 | **P9 feasibility study (no code) — Settings & Power UX.** Multi-agent investigation (3 parallel deep-dives → 3 adversarial verifications → synthesis; ~429K tok) over our app + BSP + factory_firmware + LVGL 8.4 + OSHW schematic. Findings: **DEF1 charging bug** = `bsp_system_is_charging` reads `CHRG_DET` (wrong/inverted); fix = `VBUS_IN_DET==0` (confirmed). **True power-off** = `bsp_system_shutdown()` cuts `BSP_PWR_SYSTEM`; wake = wheel-3 s long-press / USB (schematic-verified recoverable, no reflash); USB-present = no-op→relabel Reboot. **Settings 5th screen** feasible via LVGL editable-vs-screen group model (press-to-edit; group-wide editing flag → non-sticky given ≥2 members); MUST-FIX reprovision-long-press-on-slider + brightness-only NVS persist. Tasks T20–T23, design 02 §12, reqs FR14–16/DEF1. |
| 2026-06-24 | **✅ P7 (T14 stale/backoff + T15 battery/brightness) — DONE & verified.** Poll loop reworked (`main.c`): one-shot `wifi_sta_connect` then the WiFi handler **auto-reconnects forever** in the background (`wifi_is_connected()` exposes live link state) — fixes the latent re-init crash from the old retry-loop. On fetch fail: keep last-known arcs/%, show **"stale Xm"** (orange, `ui_set_stale`, age via `esp_timer`), WiFi icon red when offline, service health keeps last-known. **429 backoff:** `usage_t.http_status` added; interval doubles to a 600 s cap on 429, resets on success. **Battery (real):** `bsp_battery_is_present`/`bsp_battery_get_percent`/`bsp_system_is_charging` each poll → `ui_set_battery` (screenshot: green charging bolt on USB). **Brightness:** configurable static `cfg.brightness` (portal field, default 80) → `bsp_lcd_brightness_set` at boot. Config gained `brightness` (NVS `bri`). Fixes: `wifi_connect.h` needed `<stdbool.h>`. On-device: config load, WiFi, live 5h 7%/7d 12%, charging icon all confirmed; 47% free. **Earlier "no BSP battery API" assumption was wrong — the APIs exist.** |
| 2026-06-24 | **P6 fix — portal "Saved ✓" mojibake ("Saved âœ“").** The DONE page's UTF-8 checkmark was decoded as Latin-1 by the phone (no charset declared). Fix: `Content-Type: text/html; charset=utf-8` on both portal responses + `<meta charset='utf-8'>` in the FORM/DONE `<head>` (`provisioning.c`). Cosmetic; applies on next provision. Built/flashed; device unaffected (kept NVS config, boots to gauges). |
| 2026-06-24 | **P6 fix — captive-portal form "header fields too long".** User hit it on real-phone submit. Cause: `esp_http_server` default `CONFIG_HTTPD_MAX_REQ_HDR_LEN=512` is smaller than a phone browser's request-header block (UA + Accept* + `sec-ch-ua*` + cookies) → rejected before `/save` runs (token is in the POST *body*, not the issue). Fix: `CONFIG_HTTPD_MAX_REQ_HDR_LEN=2048` + `CONFIG_HTTPD_MAX_URI_LEN=1024` in sdkconfig.defaults (regen). **✅ User-confirmed: full phone round-trip now works — form saved & device rebooted into the gauges.** P6 fully validated incl. real-phone provisioning. |
| 2026-06-24 | **✅ P6 (T6 provisioning + T13 secure store) — DONE & verified.** Boot-and-go AES-256-GCM (no PIN) + phone captive-portal provisioning (user decisions). New modules: `secure_store.c/.h` (mbedTLS AES-256-GCM; key=SHA-256(MAC‖salt‖pepper); blob `[salt‖nonce‖tag‖ct]`), `config.c/.h` (NVS `wcfg`; token+wifi-pass in the GCM blob, SSID/ver/poll plaintext), `provisioning.c/.h` (open SoftAP `Watcher-Setup` + captive UDP-DNS + `esp_http_server` form → `/save` → reboot). Refactors: `claude_usage` takes token/version at runtime (`claude_usage_set_auth`); `wifi_connect` → `wifi_sta_connect(ssid,pass)` + `wifi_ap_start`; `main.c` orchestrates load→(provision|STA+poll); `ui_show_setup()` + `ui_screenshot_one()`. Re-provision: knob 3 s long-press (encoder `long_press_time=3000`) or knob-held-at-boot → `config_factory_reset` (polarity verified: released=high, `low-count 0/5`). **Bug fixed:** provisioning call-chain overflowed the 3.5 KB main-task stack → `CONFIG_ESP_MAIN_TASK_STACK_SIZE=8192` (regen sdkconfig) + moved screenshot's 1 KB `ob[]` to `static`. **Verified:** SoftAP+DNS+httpd up @192.168.4.1, SETUP screen rendered; crypto roundtrip survives reboot (`loaded config … token=108 chars`), STA from NVS creds → live `5h 4% / 7d 12%`, gauges render. Final clean build (`P6_SEED_FROM_SECRETS 0`) has **no secrets**; 47% free. `secrets.h` now unused by the build. |
| 2026-06-24 | **✅ T18 (FR13) splash/ambient animation — DONE & screenshot-verified.** New 4th knob screen **CLAWD** rendering a brand-terracotta claudepix Clawd creature. `main/splash.c/.h`: 20×20 palette-indexed engine ported from Clawdmeter's `splash.cpp` to LVGL 8.4 + ESP-IDF — 400×400 `lv_canvas` (`LV_IMG_CF_TRUE_COLOR`) in PSRAM, an `lv_timer` (~30 Hz, runs in the LVGL task so it's lock-safe; renders only while CLAWD is the active screen) advances frames by per-frame `hold` and auto-rotates ~20 s within the current mood group. **Mood = usage level** `max(pct_5h,pct_7d)` → 4 groups (idle<25 / normal<50 / active<80 / heavy≥80) resolved by anim name, driven from `ui_update`→`splash_set_level`. Data: reused the reference `convert_to_c.js` on the **already-vendored** `references/Clawdmeter/tools/claudepix_data/*.json` (13 anims) → `main/splash_animations.h` (177 KB; **no network scrape needed**). Palettes stay standard RGB565 and convert via `lv_color_make` at runtime → **byte-swap-agnostic** (correct under `LV_COLOR_16_SWAP`); brand remap `#cd7f6a→#d97757` kept. Wired into `ui.c` (added to the `lv_group` after SERVICE + focus-anim nav; screenshot list now 4). **Host fix:** `grab_screens.py` read-loop cap was hardcoded `len(frames)<3` → silently dropped the 4th frame; bumped to `<4`. (Firmware was correct all along — the 3-frame truncation was host-side, found via temporary `shot` ESP_LOG diag in `screenshot.c`, since reverted.) Build clean, 50% free; flashed COM18; all 4 screens captured, CLAWD creature confirmed. |
| 2026-06-24 | **✅ T17b — WiFi/battery icons repositioned (screenshot-verified).** `add_topbar()` in `ui.c`: icon `lv_obj_align` y-offset 22 → **64** (`TOP_MID`, x ±24 unchanged). The 270° ring (outer r=180, width 24 → inner r=156) draws its top arc at y≈26–54, so the old y=22 sat on it; y=64 drops both icons into the open hub, clear of the arc on the gauges and above the title (y≈140) / SERVICE label (y≈114). Built (53% free), flashed COM18, captured all 3 via `grab_screens.py` → no overlap. **"Pale WiFi on SERVICE" was a perception artifact** — pixel-sampled all 3 shots: WiFi = identical green `(74,174,82)` on 5H/7D/SERVICE; battery = grey `(156,157,156)` everywhere (correct P7 placeholder). No recolor defect. |
| 2026-06-24 | **P5 iter-1 flashed → rendered then FROZE.** Live data OK (5h 27%, 7d 9%). But serial: `lcd_panel.io.spi: spi transmit (queue) color failed` on the first full flush (coincided with WiFi init) → LVGL flush stuck → `task_wdt: LVGL task`. **Root cause:** LVGL SPI flush + WiFi both on CPU0 (contention). **Fix (per factory_firmware):** `CONFIG_LVGL_PORT_TASK_AFFINITY_CPU1=y` + `FREERTOS_HZ=1000` + `LV_MEM_CUSTOM=y`. Rebuilding. |

## 5. Validation & Test Notes
- **P1:** visually confirm the round screen draws and the knob changes something on-device.
- **P3:** compare device 5h/7d % against Claude Code's own `/usage` output (should match within a poll).
- **P4:** trigger/observe a known incident state, or mock, to confirm status parsing.
- **P6:** dump flash and confirm the token is not recoverable without the PIN.

## 6. Backlog / Ideas
- Animated "fuel gauge" / mascot per usage rate (Clawdmeter-style).
- Multiple themes for the round screen.
- Optional: confirm panel color depth and tune assets accordingly.
