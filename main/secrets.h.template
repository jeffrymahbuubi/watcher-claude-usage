#pragma once
/*
 * Secrets — LOCAL ONLY. Copy this file to `secrets.h` (same folder) and fill it in.
 * `secrets.h` is git-ignored; never commit real values.
 * Note: the ESP32-S3 is 2.4 GHz only — a 5 GHz SSID will not connect.
 */

/* 2.4 GHz WiFi */
#define WIFI_SSID     "YOUR_2G4_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"

/* Claude Code OAuth token from `claude setup-token`. */
#define CLAUDE_OAUTH_TOKEN   "PASTE_OAUTH_TOKEN_HERE"
/* The version reported by `claude --version` (used for the required User-Agent). */
#define CLAUDE_CODE_VERSION  "1.0.0"
